package com.adventures.designpatterns.proxy;

public class ProxyPattern3 {

	//This is just a explanation of a real scenario worked
	
	/*
	 * 1. A Service class which will acquire a lock
	 * 2. A proxy class which will call the service class and returns the lock object
	 */
}
